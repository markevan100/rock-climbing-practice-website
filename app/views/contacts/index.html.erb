<h1>Current contacts</h1>
<p>
  <%= link_to "Create new contact", new_contact_path %>
</p>

<!-- <table>
  <tr>
    <th>Title</th>
    <th>Name</th>
    <th>Email</th>
    <th>Cell</th>
    <th></th>
  </tr>

  <% @contacts.each do |contact| %>
  <tr>
    <td><%= contact.title %></td>
    <td><%= link_to contact.name, contact_path(contact) %></td>
    <td><%= contact.email %></td>
    <td><%= contact.cell %></td>

  </tr>
  <% end %>
</table> -->

<div class="d-flex flex-row">
  <% @contacts.each do |contact| %>

  <div class="p-2"><b><%= contact.title %></b></div>
  <div class="p-2"><%= link_to contact.name, contact_path(contact) %></div>
  <div class="p-2"><%= contact.email %></div>
  <div class="p-2"><%= contact.cell %></div>
  <br>
  <br>
  <% end %>
</div>
